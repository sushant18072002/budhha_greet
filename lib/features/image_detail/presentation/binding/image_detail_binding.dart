import 'package:get/get.dart';

import '../../../home/presentation/screens/tem.dart';

class ImageDetailBinding implements Bindings {
  @override
  void dependencies() {
    // Core Controllers
    //Get.lazyPut<TemplateDetailController>(() => TemplateDetailController());
    //Get.lazyPut<CreatorController>(() => CreatorController());
  }
}